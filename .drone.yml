kind: pipeline
type: docker
name: beech/docker/ubi8/python36

steps:
  - name: debug
    image: python:3.6
    commands:
      - ls -al /var/log/
      - ls -alh /var/log/dpkg.log
      - /bin/cat /var/log/dpkg.log
      - /bin/false
